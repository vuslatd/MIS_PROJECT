print('**HESAP MAKİNESİNE HOŞGELDİNİZ**')
print("""İŞLEMLER
1 = TOPLAMA
2 = ÇIKARMA
3 = ÇARPMA
4 = BÖLME
5 = ÜST ALMA
6 = KAREKÖK
7 = KARESİNİ HESAPLAMA
8 = MUTLAK DEĞER 
9 = FAKTÖRİYEL  
10 = LOGARİTMA 
11 = TRİGONOMETRİ
12 = MOD ALMA 
13 = ÇIKIŞ """)
print("*"*150)
print("*"*150)
while True:
    calculation = input('LÜTFEN YAPMAK İSTEDİĞİNİZ İŞLEM NUMARASINI GİRİNİZ : ')

    if calculation == '13':
        print('HESAP MAKİNESİNDEN ÇIKILIYOR ...')
        break

    elif calculation == '1':
        ineffective_element = 0
        while True:
            add = float(input('TOPLAMAK İSTEDİĞİNİZ SAYILARI GİRİNİZ  (Çıkmak için 0 a basınız):'))
            if add == 0:
                break
            ineffective_element += add
        print('TOPLAMA İŞLEMİNİN SONUCU: ', ineffective_element)

    elif calculation == '2':
        boolean = True
        ineffective_element = 0
        while True:
            subtract = float(input('ÇIKARMAK İSTEDİĞİNİZ SAYILARI SIRAYLA GİRİNİZ  (Çıkmak için 0 a basınız):'))
            if subtract == 0:
                break
            elif boolean == True:
                ineffective_element += subtract
                boolean = False
            else:
                ineffective_element -= subtract
        print('ÇIKARMA İŞLEMİNİN SONUCU : ', ineffective_element)

    elif calculation == '3':
        ineffective_element = 1
        while True:
            multiply = float(input('ÇARPMAK İSTEDİĞİNİZ SAYILARI GİRİNİZ  (Çıkmak için 1 a basınız):'))
            if multiply == 1:
                break
            ineffective_element *= multiply
        print('ÇARPMA İŞLEMİNİN SONUCU: ', ineffective_element)

    elif calculation == '4':
        boolean = True
        ineffective_element = 0
        while True:
            divide = float(input('BÖLMEK İSTEDİĞİNİZ SAYILARI SIRAYLA GİRİNİZ  (Çıkmak için 0 a basınız):'))
            if divide == 0:
                break
            elif boolean == True:
                ineffective_element += divide
                boolean = False
            else:
                ineffective_element /= divide
        print('BÖLME İŞLEMİNİN SONUCU: ', ineffective_element)

    elif calculation == '5':
        a = int(input('TABAN YAZINIZ: '))
        b = int(input('ÜST YAZINIZ: '))
        if a == 0 and b == 0:
            print('BELİRSİZ !')
        else:
            print('{} * {} İŞLEMİNİN SONUCU: {}'.format(a, b, a * b))

    elif calculation == '6':
        a = int(input('KAREKÖKÜNÜ HESAPLAMAK İSTEDİĞİNİZ SAYIYI GİRİNİZ: '))
        print('{} SAYISININ KAREKÖKÜ: {} '.format(a, a ** 0.5))

    elif calculation == '7':
        a = int(input('KARESİNİ HESAPLAMAK İSTEDİĞİNİZ SAYIYI GİRİNİZ: '))
        print('{} SAYISININ KARESİ: {}'.format(a, a ** 2))

    elif calculation == '8':
        a = int(input('İLK SAYIYI GİRİNİZ: '))
        b = int(input('İKİNCİ SAYIYI GİRİNİZ '))
        c = a + b
        if c < 0:
            absolute_value = c * -1
            print('MUTLAK DEĞERİN SONUCU: ', absolute_value)
        else:
            print('MUTLAK DEĞERİN SONUCU: ', c)

    elif calculation == '9':
        a = int(input('FAKTÖRİYELİNİ HESAPLAMAK İSTEDİĞİNİZ SAYIYI GİRİNİZ: '))
        factorial = 1
        if a < 0:
            print("NEGATİF SAYILARIN FAKTÖRİYELİ HESAPLANAMAZ.")
        else:
            for i in range(1, a + 1):
                factorial *= i
            print('SAYININ FAKTÖRİYELİ: ', factorial)

    elif calculation == '10':
        import math
        a = int(input("LÜTFEN 0'DAN BÜYÜK BİR SAYI GİRİNİZ:"))
        while (True):
            if a <= 0:
                print("LÜTFEN 0'DAN BÜYÜK BİR SAYI GİRİNİZ:")
                break
            else:
                print(math.log10(a))
                break
